/**
 * eZmax API Definition (Full)
 * This API expose all the functionnalities for the eZmax and eZsign applications.
 *
 * The version of the OpenAPI document: 1.1.18
 * Contact: support-api@ezmax.ca
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CommonReportcell from './CommonReportcell';

/**
 * The CommonReportrow model module.
 * @module eZmaxAPI/model/CommonReportrow
 * @version 1.1.18
 */
class CommonReportrow {
    /**
     * Constructs a new <code>CommonReportrow</code>.
     * A row in a Reportsubsectionpart 
     * @alias module:eZmaxAPI/model/CommonReportrow
     * @param a_objReportcell {Array.<module:eZmaxAPI/model/CommonReportcell>} 
     * @param iReportrowHeight {Number} The reportrow height in pixels
     */
    constructor(a_objReportcell, iReportrowHeight) { 
        
        CommonReportrow.initialize(this, a_objReportcell, iReportrowHeight);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, a_objReportcell, iReportrowHeight) { 
        obj['a_objReportcell'] = a_objReportcell;
        obj['iReportrowHeight'] = iReportrowHeight;
    }

    /**
     * Constructs a <code>CommonReportrow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:eZmaxAPI/model/CommonReportrow} obj Optional instance to populate.
     * @return {module:eZmaxAPI/model/CommonReportrow} The populated <code>CommonReportrow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CommonReportrow();

            if (data.hasOwnProperty('a_objReportcell')) {
                obj['a_objReportcell'] = ApiClient.convertToType(data['a_objReportcell'], [CommonReportcell]);
            }
            if (data.hasOwnProperty('iReportrowHeight')) {
                obj['iReportrowHeight'] = ApiClient.convertToType(data['iReportrowHeight'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CommonReportrow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CommonReportrow</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CommonReportrow.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['a_objReportcell']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['a_objReportcell'])) {
                throw new Error("Expected the field `a_objReportcell` to be an array in the JSON data but got " + data['a_objReportcell']);
            }
            // validate the optional field `a_objReportcell` (array)
            for (const item of data['a_objReportcell']) {
                CommonReportcell.validateJSON(item);
            };
        }

        return true;
    }

/**
     * @return {Array.<module:eZmaxAPI/model/CommonReportcell>}
     */
    getAObjReportcell() {
        return this.a_objReportcell;
    }

    /**
     * @param {Array.<module:eZmaxAPI/model/CommonReportcell>} a_objReportcell
     */
    setAObjReportcell(a_objReportcell) {
        this['a_objReportcell'] = a_objReportcell;
    }
/**
     * Returns The reportrow height in pixels
     * @return {Number}
     */
    getIReportrowHeight() {
        return this.iReportrowHeight;
    }

    /**
     * Sets The reportrow height in pixels
     * @param {Number} iReportrowHeight The reportrow height in pixels
     */
    setIReportrowHeight(iReportrowHeight) {
        this['iReportrowHeight'] = iReportrowHeight;
    }

}

CommonReportrow.RequiredProperties = ["a_objReportcell", "iReportrowHeight"];

/**
 * @member {Array.<module:eZmaxAPI/model/CommonReportcell>} a_objReportcell
 */
CommonReportrow.prototype['a_objReportcell'] = undefined;

/**
 * The reportrow height in pixels
 * @member {Number} iReportrowHeight
 */
CommonReportrow.prototype['iReportrowHeight'] = undefined;






export default CommonReportrow;

