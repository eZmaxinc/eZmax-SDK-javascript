/**
 * eZmax API Definition (Full)
 * This API expose all the functionnalities for the eZmax and eZsign applications.
 *
 * The version of the OpenAPI document: 1.2.0
 * Contact: support-api@ezmax.ca
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WebhookRequestCompound from './WebhookRequestCompound';

/**
 * The WebhookCreateObjectV2Request model module.
 * @module eZmaxAPI/model/WebhookCreateObjectV2Request
 * @version 1.2.0
 */
class WebhookCreateObjectV2Request {
    /**
     * Constructs a new <code>WebhookCreateObjectV2Request</code>.
     * Request for POST /2/object/webhook
     * @alias module:eZmaxAPI/model/WebhookCreateObjectV2Request
     * @param a_objWebhook {Array.<module:eZmaxAPI/model/WebhookRequestCompound>} 
     */
    constructor(a_objWebhook) { 
        
        WebhookCreateObjectV2Request.initialize(this, a_objWebhook);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, a_objWebhook) { 
        obj['a_objWebhook'] = a_objWebhook;
    }

    /**
     * Constructs a <code>WebhookCreateObjectV2Request</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:eZmaxAPI/model/WebhookCreateObjectV2Request} obj Optional instance to populate.
     * @return {module:eZmaxAPI/model/WebhookCreateObjectV2Request} The populated <code>WebhookCreateObjectV2Request</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebhookCreateObjectV2Request();

            if (data.hasOwnProperty('a_objWebhook')) {
                obj['a_objWebhook'] = ApiClient.convertToType(data['a_objWebhook'], [WebhookRequestCompound]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebhookCreateObjectV2Request</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebhookCreateObjectV2Request</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WebhookCreateObjectV2Request.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['a_objWebhook']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['a_objWebhook'])) {
                throw new Error("Expected the field `a_objWebhook` to be an array in the JSON data but got " + data['a_objWebhook']);
            }
            // validate the optional field `a_objWebhook` (array)
            for (const item of data['a_objWebhook']) {
                WebhookRequestCompound.validateJSON(item);
            };
        }

        return true;
    }

/**
     * @return {Array.<module:eZmaxAPI/model/WebhookRequestCompound>}
     */
    getAObjWebhook() {
        return this.a_objWebhook;
    }

    /**
     * @param {Array.<module:eZmaxAPI/model/WebhookRequestCompound>} a_objWebhook
     */
    setAObjWebhook(a_objWebhook) {
        this['a_objWebhook'] = a_objWebhook;
    }

}

WebhookCreateObjectV2Request.RequiredProperties = ["a_objWebhook"];

/**
 * @member {Array.<module:eZmaxAPI/model/WebhookRequestCompound>} a_objWebhook
 */
WebhookCreateObjectV2Request.prototype['a_objWebhook'] = undefined;






export default WebhookCreateObjectV2Request;

